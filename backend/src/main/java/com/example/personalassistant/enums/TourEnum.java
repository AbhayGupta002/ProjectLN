package com.example.personalassistant.enums;


public enum TourEnum {
    PENDING,      // booking created, not confirmed
    CONFIRMED,    // payment done
    CANCELLED,    // cancelled before tour
    COMPLETED,
}
